//! Ready Reckoner — `rr-etl` (native binary; not built for wasm32).
//! Scaffold; the owning workstream replaces it.
#![forbid(unsafe_code)]

fn main() {
    println!("rr-etl scaffold; see docs/DESIGN.md");
}
