//! Ready Reckoner — `rr-cli` (native binary; not built for wasm32).
//! Scaffold; the owning workstream replaces it.
#![forbid(unsafe_code)]

fn main() {
    println!("rr-cli scaffold; see docs/DESIGN.md");
}
