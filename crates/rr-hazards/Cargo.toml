[package]
name = "rr-hazards"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Ready Reckoner: location + household -> per-hazard household event rates, profiles and named scenarios"

[dependencies]
rr-types = { path = "../rr-types" }
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
# The national data pack in `data/`, for the every-county and backtest tests (tests/pack.rs).
rr-data = { path = "../rr-data" }
